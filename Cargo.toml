[workspace]
resolver = "2"
members = [
    "policy",
    "ipc",
    "ipc-messages",
    "worker",
    "broker",
    "tests",
    "linux-entrypoint",
    "bindings/C/broker",
    "bindings/C/worker",
    "tests/dummy-library",
]

[profile.release]
lto = true
